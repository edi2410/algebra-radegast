# ─── Basic App Settings ─────────────────────────────────────────────
PROJECT_NAME=TestRadegastApp
SECRET_KEY=testsecret123
ENVIRONMENT=testing

# ─── Database (SQLite for testing, Postgres for prod) ───────────────
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password
POSTGRES_DB=test_db

# Full SQLAlchemy URI for in-memory SQLite
SQLALCHEMY_DATABASE_URI=sqlite:///:memory:

# ─── Superuser credentials for init_db() ────────────────────────────
FIRST_SUPERUSER=test@example.com
FIRST_SUPERUSER_PASSWORD=test1234
